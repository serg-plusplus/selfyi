{
  "name": "selfyi",
  "version": "0.1.0",
  "private": true,
  "description": "Selfie — humans-only TikTok-style video app: World ID selfie-check gate, Cloudflare Workers + D1 + KV + Stream backend, Expo mobile client, tRPC end-to-end.",
  "packageManager": "pnpm@10.11.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rimraf node_modules"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  }
}
